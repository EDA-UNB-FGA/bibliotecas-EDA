#include <stdio.h>
#include <stdlib.h>
#define MAX(A,B) (A>=B)? A : ;

int get_altura(node *root){
	int cont=0;
	cont = MAX(get_altura,cont);
	return 1;
}

node* insert_heap(

int main(){

 return 0;
}
